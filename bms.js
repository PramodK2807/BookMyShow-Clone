function scroll(){
    var left = document.querySelectorAll(".scroll-img");
    left.scrollBy(350,0)
}

function scrollr(){
    var right = document.querySelectorAll(".scroll-img");
    right.scrollBy(350,0)
}